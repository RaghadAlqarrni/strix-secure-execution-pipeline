# F01 implementation record — candidate, not release-ready

1. Decision: make raw evidence mandatory in both production entrypoints. Missing raw directory, any of the twelve required files, a non-regular file or a directly symlinked required path cannot yield E9 success. Existing raw semantic checks remain in force. No fixture mode exists on the standalone E9 verifier.
2. Synthetic summary validation is explicitly separate: auditor --fixture-mode emits FIXTURE_VALIDATED with exit 3, or FIXTURE_REJECTED with exit 1. It never emits RESULT: CERTIFIED and never exits 0. Production gate invocation does not request this mode. The pure summary helper carries no enforcement-result marker.
3. Existing synthetic tests were migrated to this result vocabulary without weakening their claim predicates. Honest fixtures now require exit 3 and the explicit marker; forged fixtures require exit 1 and the rejection marker. Missing output or interpreter failure cannot masquerade as a rejected fixture. Fixture-generation failure aborts the battery.
4. New tests cover absent argument/directory, empty directory, each required raw file removed, directory substituted for a file, summary-only fixture isolation, positive raw evidence and a receiver contradiction even after its claimed hash is recomputed. Ten test methods include twelve per-file deletion subcases.
5. Existing source mutation still fails E5 hash binding. The candidate verifier hash in audit_manifest_required.json was updated because implementation bytes changed. This is a development identity, not independently signed provenance or an owner signature on historical artifacts.
6. Offline historical replay: baseline accepts complete and raw-missing inputs; candidate accepts complete inputs and rejects the missing-raw case with exit 1. This demonstrates the old false-pass path and its repair while preserving the positive control. Raw input was replayed, not freshly acquired.
7. Local results: new tests 10/10; existing fixtures 33/33; verifier self-tests 20/20; E9 summary falsification 16/16; raw contradictions 5/5; source mutation detected; ordering 3/3; audit tests 18/18. The complete Windows battery remains FAIL because the unchanged precondition test expects OTHER:ECONNRESET instead of OTHER:WSAECONNRESET (40/41). No Linux result is inferred.
8. Test source normalization uses explicit UTF-8/LF where fixtures are serialized. The real stamper can still rewrite one generated fixture with native Windows newlines; after testing, its JSON semantic equality was checked and only its baseline bytes were restored. No fixture claim was altered. Original C6 files remain 254/254.
9. Scope limits: this repair does not authenticate acquisition against a malicious trusted host, sign manifests, prove parser completeness, close arbitrary file-replacement races, implement V10, or repair F02–F10. The broader release identity/acquisition requirements remain open. These are implementer checks, not independent certification.
10. Review input: source diff plus test-results/f01-replay-results.json, baseline/candidate replay logs, and the final fixture-battery log. Required next validation: independent examination of both code and changed fixture vocabulary, then target-Linux tests. F03/F02 joint lifecycle design is the next implementation preparation.
